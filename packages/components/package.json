{
  "name": "@nighttrax/components",
  "version": "1.0.0",
  "main": "dist/components/src/index.js",
  "types": "dist/components/src/index.d.ts",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -b tsconfig.build.json && tsc-alias -p tsconfig.build.json"
  },
  "peerDependencies": {
    "react": "~18.2.0"
  },
  "devDependencies": {
    "@types/react": "~18.0.0",
    "react": "~18.2.0",
    "react-dom": "~18.2.0",
    "rimraf": "~3.0.2"
  }
}
